# Nexus

## Tauri
Rodar no fedora: GDK_BACKEND=x11 WEBKIT_DISABLE_COMPOSITING_MODE=1 npm run tauri:dev
Rodar no windows npm run tauri:dev

Para rodar no windows deve ter o visual studio devtools 2026 instalado
